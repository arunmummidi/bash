# openbox window manager config
snap install openbox
apt install lxterminal
apt install x11-server-utils

# openbox window manager config
Add application entries in
/etc/xdg/openbox/menu.xml

# set up light weight openbox window server
# Add below property to ~/.config/openbox/rc.yaml
# to open all apps maximized
    <application class="*">
        <maximized>yes</maximized>
    </application>

ssh-keygen

# Turn off a display
xrandr --listmonitors
xrandr --output LVDS-1 --off

# Skip waiting for network configuration
# during boot for unused eth interface
# Remove the configuration of unused
# interface from netplan
/etc/netplan/00-installer-config.yaml.original
netplan generate
netplan apply

# Change display brightness persistently
/sys/class/backlight/intel_backlight/brightness
